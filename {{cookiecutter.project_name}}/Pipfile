[[source]]

url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"


[packages]



[dev-packages]

pytest = "*"
pytest-cov = "*"
"pytest-flake8" = "*"
mypy = "*"
{% if cookiecutter.docs == 'y' -%}
sphinx = "*"
sphinx-autobuild = "*"
{% endif -%}
ipython = "*"
bumpr = "*"

[requires]

python_version = "{{cookiecutter.python_version}}"

[scripts]

test = "pytest --flake8"

docs = "sphinx-autobuild --open-browser --port 0 --delay 1 docs docs/_build/html"
# Equivalent of "make mode": sphinx-build -M <builder> ...
doctest = "sphinx-build -b doctest -d docs/_build/doctrees docs docs/_build/doctest"
build-docs = "sphinx-build -b html -d docs/_build/doctrees -W docs docs/_build/html"
