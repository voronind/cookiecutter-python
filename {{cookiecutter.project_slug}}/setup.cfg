[tool:pytest]
testpaths = tests
norecursedirs =
    .*/
    doc/

[flake8]
max-line-length = {{cookiecutter.python_max_line_length}}
exclude =
    .git/
    .venv/
    doc/

[coverage:run]
branch = True
source =
    project/

[mypy]
check_untyped_defs = yes
